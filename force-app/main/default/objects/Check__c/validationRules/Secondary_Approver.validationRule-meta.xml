<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Secondary_Approver</fullName>
    <active>true</active>
    <description>Req. fields when filling in Secondary Approver</description>
    <errorConditionFormula>AND(  
  ISPICKVAL(PRIORVALUE(Status__c ), &#39;Secondary Approval Needed&#39;),
  TEXT(Status__c ) = &#39;Approved&#39;,
  OR(ISBLANK(Secondary_Approval_Date__c),
  ISBLANK(Secondary_Approver__c))
)</errorConditionFormula>
    <errorMessage>Secondary Approver and Secondary Approval Date is required</errorMessage>
</ValidationRule>
